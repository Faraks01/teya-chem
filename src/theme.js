import {createTheme} from "@mui/system";

export const mainTheme = createTheme({
  palette: {
    colors: {
      purple: "#C67EFE",
      blurple: "#3D3AD1",
      gray93: "#EDEDED",
      gray72: "#B8B8B8",
      gray33: "#545454",
      lightGray: "#E2E2E2",
      nero: "#1E1E1E",
      white: "#FFFFFF"
    }
  },
});